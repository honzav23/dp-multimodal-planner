# Multimodal planner

## Installation of osmnx
1. Install Anaconda https://docs.anaconda.com/anaconda/install/
2. Run `conda create -n ox -c conda-forge --strict-channel-priority osmnx` to get osmnx package
with all of its dependencies